# Telegram Bot Token (required)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Allowed user IDs (comma-separated, required)
ALLOWED_USERS=123456789,987654321

# Multiplexer backend (optional, "tmux" or "zellij", defaults to "tmux")
MULTIPLEXER=tmux

# Multiplexer session name (optional, defaults to "ccbot")
# MUX_SESSION_NAME takes priority; TMUX_SESSION_NAME is a fallback
MUX_SESSION_NAME=ccbot

# Claude command to run in new windows (optional, defaults to "claude")
CLAUDE_COMMAND=claude

# Monitor polling interval in seconds (optional, defaults to 2.0)
MONITOR_POLL_INTERVAL=2.0

# Notification mode (optional, "all" or "interactive", defaults to "all")
# "interactive" only sends questions and permission prompts to Telegram
NOTIFY_MODE=all

# Show user message echo in Telegram (optional, defaults to "true")
SHOW_USER_MESSAGES=true

# Starting path for /pathselect directory browser (optional, defaults to bot cwd)
# BROWSE_START_PATH=/home/user/projects
